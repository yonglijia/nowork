module.exports = {
  workHoursOptions: Array.from({ length: 12 }).map((_, i) => i + 1),
  taskCountOptions: Array.from({ length: 10 }).map((_, i) => i + 1),
  extraOptions: ["主动多做事", "忍了情绪", "顾家/带娃"]
}

